\documentclass[fleqn]{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{amsmath, amssymb}
\usepackage{pdflscape}
\usepackage{nopageno}
\usepackage[margin=1in]{geometry} % Sets all margins (left, right, top, bottom) to 1 inch
%\usepackage{bidi}       % Provides RTL text support


\begin{document}

\begin{landscape}
%		\[[\text{ADRP model}]\,\,\, \text{Min} :\max_m \left\{ T_{(m,i_d)}^Q + G_{(m,i_d)}^Q , T_{(m,i_d)}^Y + G_{(m,i_d)}^Y \right\}
%	\]

	% ۱. $m$:
	% اندیس \lr{QC} ها
	
	% ۲.  $(m,i)$: اندیس \lr{i}-امین کار کانتینری \lr{QC} \lr{m}
	
	% ۳.  $W^H$: مجموعه ی اعمال در راستای افق = $\{(m,i,\alpha) | \alpha \in \{2\}\}$
	
	% ۴.  $W^T = w^H \bigcup W^V$
	
	% ۵.  $C = D \bigcup L$ مجموعه ی تمام کانتینر ها
	
	% ۶. $l ,\,\,\ AGVs $ تعداد 
	
	% ۷. مسیر های مسئله:
	
	% الف: $X^R$
	
	% ب: $Y^R$
	
	% ج: $Y^S$==
	
	% د: $Y^L$
	
	% ۸: سرعت $AGV$ ها یکنواخت و ثابت است
	
	% %- $G_{(m,i_d)}^Q$: زمان مورد نیاز \lr{QC} برای آخرین کار کانتینری
	
	% %- $T_{(m,i_d)}^Y$: زمان شروع برای \lr{AGV} 
	% %جهت قرار دادن (برداشتن) آخرین کانتینر بر روی (از) \lr{AGV-Support}
	% %
	% %- $G_{(m,i_d)}^Y$: زمان مورد نیاز برای \lr{AGV} 
	% %جهت قرار دادن (برداشتن) آخرین کانتینر بر روی (از) \lr{AGV-Support}

	
%\[
%	\sum_{(n,j)\in C\cup\{0\}} Z_{(n,j)(m,i),l} = \sum_{(h,k)\in C\cup\{0\}} Z_{(m,i)(h,k),l}\quad \forall(m,i)\in C, \, \forall l\in B
%\]
%\[
%	\sum_{(m,i)\in C} Z_{(m,i),0,l} = 1,\forall l \in B
%\]

%\[
%	\sum_{(m,i)\in C} Z_{0,(m,i),l} = 1, \forall l\in B
%\]

%\[
%	\sum_{l \in B} \sum_{(n,j) \in D\cup \{0\}} Z_{(m,i)(n,j),l} = 1, \forall(m,i)\in L
%\]
%\[
%	\sum_{l\in B}\sum_{(n,j)\in L\cup\{0\}} Z_{(m,i)(n,j),l} = 1, \forall (m,i)\in D
%\]
%	
%\[
%	P^X_{(m,i,4),x} = P^X_{(n,j,0),x} , \sum_{l\in B} Z_{(m,i)(n,j),l} = 1, \forall (m,i),(n,j)\in C, \forall x\in X^\mathbb{R}
%\]
%

%\[
%	\sum_{x\in X^\text{R}} P^X_{(m,i,\alpha),x} = 1, \forall (m,i)\in C, \forall \alpha \in \{0,1,2,3,4\}
%\]

%\[
%	P^X_{(m,i,0),O_{(m,i)}}=1, \forall (m,i)\in D
%\]

%\[
%	\sum_{y \in Y^L} P^Y_{(m,i,0),y} = 1, \forall (m,i)\in L
%\]

%\[
%\sum_{y \in Y^S} P^Y_{(m,i,0),y} = 1, \forall (m,i)\in D
%\]

%\[
%	\sum_{x=A^L_{(m,i)}}^{A^\text{R}_{(m,i)}} P^{X}_{(m,i,0),x} = 1, \forall (m,i)\in L
%\]

%\[
%\sum_{y\in Y^L} P^Y_{(m,i,3),y}=1, \forall(m,i)\in D
%\]

%\[
%\sum_{y\in Y^S} P^Y_{(m,i,3),y}=1, \forall(m,i)\in L
%\]

%\[
%P^X_{(m,i,3),O_{(m,i)}} =1, \forall (m,i) \in L
%\]

%\[
%	\sum_{x=A^L_{(m,i)}}^{A^\text{R}_{(m,i)}} P^{X}_{(m,i,3),x} = 1, \forall (m,i)\in D
%\]

%\[
%P^Y_{(m,i,\alpha),y} = P^Y_{(m,i,\alpha -1 ),y} , \forall (m,i,\alpha)\in W^H,\forall y\in Y^{\text{R}}
%\]

%\[
%P^X_{(m,i,\alpha),x} = P^X_{(m,i,\alpha -1 ),x} , \forall (m,i,\alpha)\in W^V,\forall y\in X^{\text{R}}
%\]

%\[
%	U^{\text{AGV}}_{(m,i,4)(n,j,1)}\ge \sum_{l\in B}Z_{(m,i)(m,j),l} \quad , \forall (m,i),(n,j)\in C
%\]

%%%%%%%%%%%%%%%%%%%%%%%%%
%\[
%U^{\text{AGV}}_{(m,i,\alpha_1)(n,j,\alpha_2)} + U^{\text{AGV}}_{(n,j,\alpha_2)(m,i,\alpha_1)} + 3
%- P^Y_{(m,i,\alpha_1),y} -P^Y_{(n,j,\alpha_2),y} -
%\sum_{x=1}^{x'}\left(P^X_{(m,i,\alpha_1 -1),x} + P^X_{(n,j,\alpha_2),x} - P^X_{(m,i,\alpha_1),x} - P^X_{(n,j,\alpha_2 -1),x} \right)\ge 0,
%\]
%\begin{RTL}
%	\hspace{1cm}		
%$	\raggedright
%\forall (m,i,\alpha_1), (n,j,\alpha_2)\in W^H \, \forall y \in Y^{\text{R}}, \,\forall x'\in X^{\text{R}}$
%
%\end{RTL}

%%%%%%%%%%%%%%%%%%%%%

%C26
%\[
%\left( 3- U^{\text{QC}}_{(m,i)(n,j,\alpha_2)}
%- P^Y_{(m,i,\alpha_1),y} - 
%P^Y_{(n,j,\alpha_2),y}
%+ \big| \sum_{x=1}^{O_{(m,i)}} P^X_{(n,j,\alpha_2),x} - \sum_{x=O_{(m,i)}+1}^{x_R} P^X_{(n,j,\alpha_2 - 1),x} \big|
%\right)M + T^{\text{Start}}_{(n,j,\alpha_2)} +
%t^{\text{AGV}}_{(n,j,\alpha_2 -1)(m,i,\alpha_1)}
%\ge T^Q_{(m,i)} + G^Q_{(m,i)}
%\]
%\begin{flalign*}	
%	&&,\forall (n,j,\alpha_2)\in W^H,\forall y\in Y^S , \forall (m,i)\in D, \forall \alpha_1\in \{0\} or \forall(m,i) \in L, \forall \alpha_1 \in \{3\} 
%%	\hspace{0.3cm}
%\end{flalign*}
%\[
%T^Q_{(m,i)} +G^Q_{(m,i)} +M \big(1-U^{\text{QC}}_{(m,i)(n,j,\alpha)}\big) \ge T^{\text{Start}}_{(n,j,\alpha)}, 
%	\forall (m,i)\in C, \forall (n,j,\alpha)\in W^H
%\]

%\[
%T^{\text{Start}}_{(n,j,\alpha)} + t^{\text{AGV}}_{(n,j,\alpha-1)(n,j,\alpha)} +
% M \big(1-U^{\text{QC}}_{(m,i)(n,j,\alpha)}\big) \ge T^{Q}_{(m,i)}, \,\, \forall (m,i)\in C, \forall (n,j,\alpha)\in W^H
%\]

%\[
%	U^{\text{AGV}}_{(m,i,\alpha_1)(n,j,\alpha_2)} + U^{\text{AGV}}_{(n,j,\alpha_2)(m,i,\alpha_1)} \ge 
%	P^X_{(m,i,\alpha_1),x} + P^X_{(n,j,\alpha_2),x} - 1,\, \forall(m,i,\alpha_1),(n,j,\alpha_2)\in W^V, \forall x\in X^R
%\]

%\[
%U^{\text{AGV}}_{(m,i,\alpha-1)}=1 , \,\, \forall (m,i)\in C, \forall\alpha\in \{2,3,4\}
%\]
%
%\[
%	T^{\text{Start}}_{(n,j,\alpha_2)} + M \Big(1 -  U^{\text{AGV}}_{(m,i,\alpha_1)(n,j,\alpha_2)}
%	\Big) \ge
%	T^{\text{Start}}_{(m,i,\alpha_1)} +t^{\text{AGV}}_{(m,i,\alpha_1 - 1)(m,i,\alpha_1)}, \,\, \forall (m,i,\alpha_1),(n,j,\alpha_2)\in W^T
%\]

%

%\[
%	T^Q_{(m,i+1)} \ge T^Q_{(m,i)} + G^Q_{(m,i)} + S^Q_{(m,i)(m,i+1)}, \forall (m,i),(m,i+1)\in C
%\]

%\[
%T^Q_{(n,j)} \ge T^Q_{(m,i)} + G^Q_{(m,i)} , \forall (m,i,n,j)\in \psi_1
%\]

%\[
%T^Y_{(n,j)} \ge T^Y_{(m,i)} + G^Y_{(m,i)} , \forall (m,i,n,j)\in \psi_2
%\]

%\[
%T^Y_{(m,i)} \ge T^{\text{Start}}_{(m,i,3)} + t^{\text{AGV}}_{(m,i,2)(m,i,3)}, \forall (m,i) \in D
%\]

%\[
%T^Q_{(m,i)} \ge T^{\text{Start}}_{(m,i,3)} + t^{\text{AGV}}_{(m,i,2)(m,i,3)}, \forall (m,i) \in L
%\]

%\[
%T^Y_{(n,j)} + M \Big(
%1- \sum_{l\in B} \ge T^{\text{Start}} + t^{\text{AGV}}_{(m,i,3)(m,i,4)}\Big), \forall(m,i)\in D, \forall (n,j)\in L
%\]


%\[
%T^Y_{(n,j)} + M \Big(
%1- \sum_{l\in B} Z_{(m,i)(n,j),l} \Big) \ge T^{\text{Start}} + t^{\text{AGV}}_{(m,i,3)(m,i,4)}, \forall(m,i)\in D, \forall (n,j)\in L
%\]
%
%
%\[
%T^{\text{Start}}_{(m,i,\alpha)} \ge T^Y_{(m,i)} + G^Y_{(m,i)}, \,\, \forall(m,i)\in D, \alpha \in \{4\} \, \, \, \, or \,\,\, \forall(m,i)\in L, \,\, \alpha \in \{1\}
%\]


%\[
%T^{\text{Start}}_{(m,i,\alpha)} \ge T^Q_{(m,i)} + G^Q_{(m,i)}, \,\, \forall(m,i)\in D, \alpha \in \{1\} \, \, \, \, or \,\,\, \forall(m,i)\in L, \,\, \alpha \in \{4\}
%\]
%
%

%\[
%X^{\text{position}}_{(m,i,\alpha)} = x, P^X_{(m,i,\alpha),x} = 1, \forall (m,i)\in C, \forall \alpha \in \{0,1,2,3,4\} , \forall x \in X^R
%\]

%\[
%Y^{\text{position}}_{(m,i,\alpha)} = y, P^Y_{(m,i,\alpha),y} = 1, \forall (m,i)\in C, \forall \alpha \in \{0,1,2,3,4\} , \forall y \in Y^R
%\]

%\[
%t^{\text{AGV}}_{(m,i,\alpha_1)(n,j,\alpha_2)} = \Big(
%\Big| 
%X^{\text{position}}_{(m,i,\alpha_1)} - X^{\text{position}}_{(n,j,\alpha_2)}
%\Big|
%+
%\Big| 
%Y^{\text{position}}_{(m,i,\alpha_1)} - Y^{\text{position}}_{(n,j,\alpha_2)}
%\Big|
%\Big)
%\Big/v^{\text{AGV}}, \forall(m,i),(n,j)\in C, \forall \alpha_1, \alpha_2\in \{0,1,2,3,4\}
%\]

%\[
%Z_{(m,i)(n,j),l}\in \{0,1\},\forall(m,i),(n,j)\in C, \forall l \in B
%\]
%

%\[
%U^{\text{AGV}}_{(m,i,\alpha_1)(n,j,\alpha_2)} \in \{0,1\}, \forall(m,i,\alpha_1)(n,j,\alpha_2)\in W^T
%\]

%\[
%U^{\text{QC}}_{(m,i)(n,j,\alpha)} \in \{0,1\}, \forall(m,i)\in C, (n,j,\alpha)\in W^H
%\]

%\[
%P^X_{(m,i,\alpha),x},P^Y_{(m,i,\alpha),y}\in \{0,1\}, \forall(m,i)\in C,\forall \alpha \in \{0,1,2,3,4\},\forall x\in X^R ,\forall y\in Y^R
%\]
%

%\[
%T^Q_{(m,i)}, T^Y_{(m,i)},T^{\text{Start}}_{(m,i,\alpha)} \ge 0, \forall(m,i)\in C, \forall \alpha \in \{1,2,3,4\}
%\]
%





















































\end{landscape}

\end{document}
